# SLG UI Framework 开发 Backlog

目标：在有限时间内，优先提升中高级 Unity 面试价值、工程质量与 Demo 视觉完成度，同时避免过度设计。

## 优先级规则

- **P0 必须修**：影响启动、核心流程、框架正确性或演示稳定性。
- **P1 下一步开发**：能显著增强面试展示与核心技术含量。
- **P2 品质提升**：改善视觉、体验、维护性和文档表达。
- **P3 暂时不做**：当前投入产出比低，容易扩大范围或导致过度设计。

## P0 必须修

### P0-1 启动与构建闭环

- [ ] 在 Build Settings 中配置可运行的 Demo 场景。
- [ ] 验证启动流程能够正确初始化 `UIManager`、`UIRoot` 和 `MainHUD`。
- [ ] 清理场景、Prefab 中的 Missing Script、空引用和启动时报错。
- [ ] 建立最小 PlayMode 冒烟测试：启动、打开 UI、关闭 UI、返回主界面。

验收标准：从编辑器和构建包启动后，无阻断性异常，能够稳定进入主界面并完成一次 UI 操作闭环。

### P0-2 修复可见的未完成交互

- [ ] 活动入口能够打开 `ActivityPanel`，并显示有效内容。
- [ ] 修复聊天界面正文或列表内容不可见的问题。
- [ ] 邮件阅读后，列表中的已读状态立即刷新。
- [ ] 清理按钮、标题和列表项中的占位文本或空文本。
- [ ] 验证主界面所有可见入口均可进入、操作并正常返回。

验收标准：演示过程中不存在明显无响应按钮、空白面板、状态不同步或占位内容。

### P0-3 UI 生命周期与状态机正确性

- [ ] 统一并验证 `Open / Show / Hide / Close` 的状态转换规则。
- [ ] 防止重复播放打开、关闭动画或重复触发生命周期回调。
- [ ] 处理打开与关闭动画互相打断时的竞态问题。
- [ ] 明确 `Release` 与 `OnDestroy` 的职责边界，保证资源只释放一次。
- [ ] 明确缓存 UI 的隐藏、复用和销毁策略。

验收标准：快速连续操作不会造成状态错乱、重复回调、空引用或残留输入拦截。

### P0-4 实例、导航与层级管理

- [ ] 单实例 UI 重复打开时复用已有实例，并正确更新参数与显示状态。
- [ ] 多实例 UI 使用稳定实例标识，支持独立关闭和释放。
- [ ] Panel 栈的压栈、返回和恢复行为一致。
- [ ] Popup 遮罩与输入拦截正确，关闭顶层弹窗后恢复下层交互。
- [ ] 对 UI 层级配置做启动时校验，避免错误层级或配置缺失静默失败。

验收标准：单例、多实例、Panel 和 Popup 混合操作时，层级、焦点、返回顺序与输入行为正确。

### P0-5 最小自动化回归测试

- [ ] 覆盖 UI 状态转换与重复调用。
- [ ] 覆盖单实例复用与多实例隔离。
- [ ] 覆盖 Panel 栈返回顺序。
- [ ] 覆盖 Popup 遮罩和输入恢复。
- [ ] 覆盖缓存复用、释放与销毁。

验收标准：核心框架行为可通过一组快速、稳定、可重复运行的测试验证。

## P1 下一步开发

### P1-1 两分钟完整 Demo 演示链

- [ ] 设计并实现“主城 HUD → 功能面板 → 详情弹窗 → 状态更新 → 返回”的完整演示流程。
- [ ] 保证演示链覆盖 Panel、Popup、Toast、导航、数据刷新等框架能力。
- [ ] 增加明确的操作反馈，使面试官无需解释也能理解流程。

### P1-2 列表性能能力

- [ ] 为至少一个高频列表实现对象池复用。
- [ ] 为长列表实现虚拟化或复用滚动方案。
- [ ] 补充简单性能对比数据：实例数、GC 分配、首开时间或滚动帧率。

### P1-3 可展示的性能与调试信息

- [ ] 记录 UI 首次打开、缓存后再次打开的耗时。
- [ ] 展示当前 UI 实例数量、缓存数量和层级信息。
- [ ] 为关键异常提供清晰日志与上下文。

### P1-4 World UI 最小切片

- [ ] 实现一个典型 SLG 世界 UI 示例，例如单位血条、名称或选中标记。
- [ ] 支持跟随、屏幕边界处理和基础对象复用。
- [ ] 只做可演示的最小切片，不扩展为完整世界地图系统。

### P1-5 最小数据流闭环

- [ ] 将一个 Demo 功能改造成“数据 → UI 列表 → 详情 → 操作 → 数据更新 → UI 刷新”的完整链路。
- [ ] 保持接口清晰，避免引入完整 MVVM、DI 容器或复杂事件总线。

## P2 品质提升

### P2-1 SLG 视觉完成度

- [ ] 统一字体、字号、颜色、按钮、间距和面板层次。
- [ ] 强化主城 HUD、资源栏、功能入口和弹窗的 SLG 视觉特征。
- [ ] 补充少量高收益图标、状态标记、选中效果和空状态。

### P2-2 动效与交互反馈

- [ ] 统一面板进入、退出和弹窗动效节奏。
- [ ] 增加按钮按下、禁用、加载和操作成功反馈。
- [ ] 限制动效数量，优先处理高频路径。

### P2-3 分辨率与安全区适配

- [ ] 验证常见桌面和移动端宽高比。
- [ ] 处理刘海屏、安全区、超宽屏和小分辨率布局。
- [ ] 建立关键界面的截图对比清单。

### P2-4 工程整理

- [ ] 统一命名、目录、序列化字段和日志格式。
- [ ] 删除无效资源、重复代码和未使用配置。
- [ ] 为核心类补充必要注释，不做大规模注释填充。

### P2-5 项目说明与面试材料

- [ ] 补充架构图、生命周期图和 UI 打开流程图。
- [ ] 记录关键设计取舍、已知限制和性能数据。
- [ ] 准备两分钟演示脚本和常见追问答案。

## P3 暂时不做

- [ ] 全面迁移 Addressables 或重做资源系统。
- [ ] 开发新的大型玩法、战斗、联盟或世界地图系统。
- [ ] 接入真实网络、服务器、账号或支付系统。
- [ ] 引入完整 MVVM、依赖注入框架或全局事件总线。
- [ ] 开发通用动效编辑器、模糊渲染系统或复杂 UI 特效管线。
- [ ] 为极端规模提前实现 ECS 或数千单位级完整优化方案。
- [ ] 在核心闭环稳定前进行全项目架构重写。

## 推荐执行顺序

1. 完成全部 P0，确保项目可启动、可演示、可回归。
2. 实现 P1-1 演示链，再完成 P1-2 列表性能与 P1-3 调试展示。
3. 根据剩余时间选择 P1-4 或 P1-5，优先补足当前 Demo 最薄弱的能力。
4. 用 P2 做视觉与表达收尾；P3 保持冻结。
